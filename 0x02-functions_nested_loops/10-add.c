#include "main.h"
/**
  * add - function that adds to integers and returns the results
  * @m: an integer
  * @c: an integer
  * Return: returns result of add m and c
  */
int add(int m, int c)
{
	return (m + c);
}
